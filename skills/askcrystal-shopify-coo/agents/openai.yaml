interface:
  display_name: "AskCrystal Shopify COO"
  short_description: "Manage AskCrystal products, media, and Shopify sync"
  default_prompt: "Use $askcrystal-shopify-coo to create or update an AskCrystal Shopify product listing, price with at least 7x markup rounded upward to .99, assign a seeded artist handle for $99.99+ products, generate default imagegen product image plans with SEO metadata, avoid raw source media unless explicitly requested, validate it, and dry-run or apply safe Shopify product upload steps."
policy:
  allow_implicit_invocation: true
